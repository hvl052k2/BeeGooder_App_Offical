import React from "react";
import {
    View,
    Text,
    StyleSheet,
    SafeAreaView,
    ScrollView,
    Image,
    TouchableOpacity,
    Alert,
    FlatList,
    ImageBackground,
  } from 'react-native';

export default AnimalPostsScreen = ()=>{
    return(
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text>Animal Posts Screen</Text>
        </View>
    )
}