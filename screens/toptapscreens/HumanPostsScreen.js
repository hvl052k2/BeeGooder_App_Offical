import React from "react";
import {
    View,
    Text,
    StyleSheet,
    SafeAreaView,
    ScrollView,
    Image,
    TouchableOpacity,
    Alert,
    FlatList,
    ImageBackground,
  } from 'react-native';

export default HumanPostsScreen = ()=>{
    return(
        <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
            <Text>Human Posts Screen</Text>
        </View>
    )
}